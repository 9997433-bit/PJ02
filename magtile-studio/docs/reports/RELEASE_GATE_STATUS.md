# 发布门禁状态报告 (Release Gate Status)

- 生成时间: 2026-08-26 03:56 UTC
- 基线提交: `9aa146d` (`cursor/magtile-studio-foundation-a95b`, 内容库 250 模型; 自上一基线 `b31e933` 以来为软件侧红项清零波次 —— QA 难度感知片数下限治理 (`b925eba`)、2 个 D5 扩规模 (`531860e`/`d78f419`)、`strait_rainbow_bridge_01` R9 放置重排 (`acdc834`)、目录同步 (`9aa146d`)、实物复核三件套刷新至 51 口径 (`e387d31`) —— C++ 引擎源码未变更, 仅 QA 脚本/CTest 挂钩/模型 JSON/文档)
- 构建配置: CMake Release, `/tmp/wt-risk-report/magtile-studio` worktree (@ `9aa146d`, 工作区干净) 增量构建 → **退出码 0**
- 执行命令:
  1. `tools/run_release_gate.sh --full --fail-on-pending` → **退出码 1** —— **全量 QA 关卡首次全绿** (42 子关卡: 39 过 + 3 可选跳过, 0 失败, 耗时 100s); 唯一红灯为 L3 实物复核 51 个待复核在 `--fail-on-pending` 硬闸门口径下红灯 (见 §4)
  2. `tools/check_v1_readiness.sh --quick` → **退出码 1** —— 25 项: 14 PASS / 2 FAIL / 9 SKIP, P0 失败仅 R6/R7 (实物复核, 用户侧)
- 口径变化 (与上一基线 `b31e933` 的差异):
  - **QA 关卡 4/5 片数下限转为难度感知** (`b925eba`, 上一报告 §3.1 两条出路中的治理决策②落地): 全量质检与反平凡检查按 CONTENT_STRATEGY.md 2.1 节难度片数带执行 D1 ≥ 20 / 其余 ≥ 40, 决策已同步 docs/TESTING.md 3.3/3.4/合入闸门节与 PHYSICS_VERIFICATION_DEEP_DIVE.md; 关卡 4 名称变更为「模型库全量质检 (难度感知片数下限)」
  - 门禁关卡表仍 22 关 / 42 子关卡, CTest 仍 556 项, `--full` 档发布专项环境变量仍 4 个全开 —— 门禁语义除上述片数下限治理外无变化; 红项从「5 个内容侧真红 + L3」收敛为「仅 L3」

## 1. 结论速览

| 关卡 | 结果 | 说明 |
| --- | --- | --- |
| 全量 QA (42 子关卡: 39 过 3 可选跳过 0 失败) | **GREEN (新)** | 上一基线的 5 个软件侧真红全部清零 (§2)。CTest 556/556; 全量质检 250/250 (难度感知下限: D1 20 片带 / 其余 40 片); 反平凡/逻辑质检/逐步装配全过; 唯一性 250 模型两两比对 0 警告; strict 巡检三阶段全绿 —— 静态档 249 过 + 1 白名单豁免 (`suspension_bridge_01`) 0 警告, 逐步装配过, **D4+ 抗扰动 51/51 × 50 轮全绿**; 免费层 30/30; 系列归类矩阵内 201 + 矩阵外 49 (缺失/非法 0); 儿童文案 301 文件 8571 段全绿; 可选跳过 3 项为教程步进性能基准 / L2 独立加闸 / 矩阵快照刷新 (均非 `--full` 档默认项); 耗时 100s |
| 难度配额守卫 (QA 关卡 41, strict) | **GREEN (保持)** | D1 20 (8.0%) / D2 23 (9.2%) / D3 156 (62.4%) / D4 45 (18.0%) / D5 6 (2.4%), 解冻线 D1 ≥ 20 与 D5 ≥ 6 保持达标, **D3 冻结解除状态维持** (§3) |
| L3 实物复核缺口 (硬闸门, `--fail-on-pending`) | **FAIL (预期, 唯一红灯)** | 扫描 250 模型, D4+ 51 个待复核 0/51 (45 D4 + 6 D5) —— 用户侧人手实搭, 非软件缺陷; 排产三件套已同步 51 口径 (§4) |
| V1 就绪快检 (`--quick`) | **FAIL (预期)** | 14 PASS / 2 FAIL / 9 SKIP; P0 失败仅 R6 (抽样包缺口 10/10, 命中 S1=0 S2=6 S3=4 —— 6 个 D5 全部进入抽样包, 预计 1000 分钟 ≈ 16.7 小时) 与 R7 (D4+ 51 待复核清零), 与 L3 同源; R1/R2/R3/R8~R16/R18 全过, R4/R5/R17 与 M1~M6 按 `--quick` 口径跳过 |

**工程侧判定: 软件门禁在 250 模型规模上首次达成 `--full --fail-on-pending` 档全绿 (除 L3)。** 上一基线 (`b31e933`) 的 5 个内容侧真红已全部按正式路径清零 (§2): 20 个 D1 片数下限冲突走治理决策落盘, 2 个 D5 片数区间与 2 个 D5 抖动失败走内容返工实修 —— 无静默放宽、无占位判绿。当前距全绿仅剩 L3 实物复核 51/51 (用户侧实搭, 排产单 `docs/reports/PHYSICAL_REVIEW_QUEUE.md`: 族去重必搭 40 ≈ 51.7h + 可缓建 11 ≈ 12.8h)。

## 2. 上一基线 5 个软件侧红项清零明细

上一基线 `b31e933` 报告 (§3) 列明 3 组共 5 个软件侧真红, 本波次逐项处置如下, 本次实跑全部转绿:

### 2.1 20 个新 D1 模型片数下限冲突 → 治理决策② (难度感知下限) 落地

- 处置 (`b925eba`): QA 关卡 4 (`test_all_models.sh`) 与关卡 5 (`test_anti_trivial.py`) 片数下限改为难度感知 —— **D1 ≥ 20** (取 CONTENT_STRATEGY.md 2.1 节 D1 片数带 [20, 28] 下沿, 2.4 节反幼稚规则: 入门档降低的是操作难度而非成品感; 片数带上限与难度合法性仍由关卡 6 逻辑质检把关), **其余难度 ≥ 40** 不变; CTest 双闸门 (`all_models_quality_gate` / `anti_trivial_models`) 同步接线, 决策记录落盘 docs/TESTING.md (3.3/3.4/9.1 合入闸门) 与 PHYSICS_VERIFICATION_DEEP_DIVE.md
- 本次验证: 关卡 4 全量质检 250/250 过 (日志头明示「最低片数要求: 40 (D1 入门档按 CONTENT_STRATEGY 片数带为 20)」), 关卡 5 反平凡 250/250 过, CTest #514/#550 转绿; 20 个 D1 (20~27 片) 全部入闸

### 2.2 2 个新 D5 模型低于片数区间 [110, 180] → 内容扩规模实修

| 模型 | 上基线 | 本基线 | 处置提交 |
| --- | --- | --- | --- |
| `giant_ferris_wheel_01` | 102 片 | **122 片 / 30 步** | `d78f419` (扩规模 + 抖动加固一体) + `9aa146d` (目录同步) |
| `marble_grand_cascade_01` | 87 片 | **123 片 / 32 步** | `531860e` |

本次验证: 关卡 6 模型逻辑质检 (CTest #551 `model_logic_gate`) 转绿, D5 全部 6 个落在 [110, 180]: 110 / 116 / 122 / 122 / 123 / 124。

### 2.3 2 个新 D5 模型 strict 抖动仿真失败 → R9 加固实修

- `giant_ferris_wheel_01`: `d78f419` 结构加固后 `validate --profile strict --jitter 50` 实跑通过
- `strait_rainbow_bridge_01`: `acdc834` 按 docs/PHYSICS_RULES.md R9 节把第 23/25 步的封闭放置片移到结构合拢之前, 50 轮零违规

本次验证: 关卡 35 strict 巡检阶段 3/3 (D4+ 抗扰动, 蒙特卡洛 ±1.5mm / ±2.0°, 50 轮) **51/51 全绿** —— 含本波次返工的 3 个 D5 与既有 R9 修复模型 (`ball_run_tower_01` / `marble_run_spiral_01` / `rainforest_canopy_01`), 零失败零豁免新增。

## 3. 难度配额守卫保持绿灯 (D3 解冻状态维持)

`--full` 档强制开启 `MAGTILE_DIFFICULTY_QUOTA=1`, QA 关卡 41 以 `check_difficulty_quota.py --strict` 运行, 本次实跑分布与判定:

| 难度 | 数量 | 占比 | 解冻线 | 较上基线 |
| --- | --- | --- | --- | --- |
| D1 (入门) | 20 | 8.0% | ≥ 20, 达标 | 不变 |
| D2 (进阶) | 23 | 9.2% | — | 不变 |
| D3 (熟练) | 156 | 62.4% | 冻结解除维持 | 不变 |
| D4 (挑战) | 45 | 18.0% | — | 不变 |
| D5 (大师) | 6 | 2.4% | ≥ 6, 达标 | 不变 (2 个扩规模不改难度) |

本波次未增删模型、未改任何难度标定 (2 个 D5 仅扩片数), 总量保持 250, strict 档退出码 0, 解冻状态与上基线一致。

## 4. L3 实物复核缺口 (硬闸门红, 唯一剩余红灯)

本次带 `--fail-on-pending`, L3 走硬闸门口径: 扫描 250 模型, D4+ 51 个全部待复核 (45 D4 + 6 D5), 门禁关卡 2 以退出码 1 结束。`check_v1_readiness.sh --quick` 对应 R6/R7 两项 P0 FAIL —— 用户侧人手实搭事项, 待按 `docs/PHYSICAL_REBUILD_CHECKLIST.md` 实搭后回填 `physical_verified`。

上一报告要求的排产文档刷新已完成 (`e387d31`): `PHYSICAL_REVIEW_QUEUE.md/.csv` (51 行: 必搭 40 / 可缓建 11)、`PHYSICAL_FAMILY_PACK.md` (族去重必搭 40 个 3100 分钟 ≈ 51.7h, 可缓建 11 个 770 分钟 ≈ 12.8h, 全集 51 个 ≈ 64.5h)、`PHYSICAL_RISK_REPORT.md/.json` (风险 Top3: `skyscraper_01` 63.9 / `stellar_launch_gantry_01` 61.8 / `strait_rainbow_bridge_01` 56.8) 均为 51 口径。R6 抽样包命中 S1=0 S2=6 S3=4 —— 6 个 D5 全部纳入优先抽样 (含 3 个本波次返工模型, 软件侧已全绿, 可直接排实搭)。

## 5. 下一步

1. **用户侧 (唯一 P0 阻塞)**: 按 `docs/USER_HANDOFF.md` §4 完成实物签核 —— L3 待复核 51 清零 (排产单 `docs/reports/PHYSICAL_REVIEW_QUEUE.md`, 建议从 R6 抽样包 10 个起步, 优先 6 个 D5); 另有行政/实机/沙盒/法务 Manual P0 项并行推进
2. **工程侧**: 无待修红项 —— 软件门禁全绿, 保持 D1 ≥ 20 / D5 ≥ 6 与 250 上限即可; 新增内容一律走 `review_content_batch.sh` 常规评审
3. **正式出包前**: 以 `tools/run_release_gate.sh --full --l2 --fail-on-pending` 复跑终防线 (本次 `--full` 档 strict 巡检阶段 3 已实跑 D4+ 抗扰动 51/51 全绿, L2 独立加闸预期同绿; 唯 L3 清零后方能整体全绿)
